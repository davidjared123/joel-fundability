# Fundability Dashboard - Environment Variables
# Copy this file to .env and fill in your values

# Supabase Configuration
# Get these from your Supabase project: Settings → API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: For the GitHub Actions keep-alive workflow, you also need to add
# these as repository secrets (without the VITE_ prefix):
#   - SUPABASE_URL (same value as VITE_SUPABASE_URL)
#   - SUPABASE_ANON_KEY (same value as VITE_SUPABASE_ANON_KEY)
#
# Add secrets at: GitHub Repo → Settings → Secrets and variables → Actions
